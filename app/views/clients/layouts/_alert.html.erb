<div data-controller="flash">
  <% flash.each do |type, message| %>
    <div data-flash-target="flashMessage" class="fixed top-16 z-50 right-5 px-4 py-2 rounded-md shadow-md <%= ApplicationHelper.flash_class(type) %>">
        <%= message %>
    </div>
  <% end %>
</div>
